{% extends "@Chat/common/template.html.twig" %}
{% trans_default_domain 'chat' %}

{% block title %}
    {{ 'site.title.chat.create'|trans({}, 'site') }}
{% endblock %}

{% block body %}
    <div class="w-full max-w-md bg-white rounded-2xl border border-gray-200 p-8 shadow-sm">

        <h2 class="text-2xl font-semibold text-gray-900 mb-6 text-center">
            {{ 'chat.form.title'|trans }}
        </h2>

        {{ component('createChatForm') }}

    </div>
{% endblock %}
